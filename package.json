{
  "name": "tg-mod-forward-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram moderated forward bot with Redis/SQLite persistence for Render.",
  "engines": {
    "node": "20.x"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "start": "node --max-old-space-size=512 dist/index.js",
    "postinstall": "npm rebuild better-sqlite3 --update-binary || true",
    "test": "npm run build && node dist/test/detectAd.spec.js",
    "sandbox:detectAd": "npm run build && node dist/scripts/detectAd-sandbox.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.5.0",
    "bottleneck": "^2.19.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
